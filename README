This software can edit Intel MSR/MMIO registers, communicate with OC/PCODE/VR mailboxes.

Currently W.I.P.

Features:
Cross-platform (Linux/Windows)
Uses a signed driver for IOCTL in Windows (works with DSE)
